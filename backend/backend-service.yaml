apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: NodePort
  selector:
    app: backend-airplane
  ports:
  - protocol: TCP
    port: 44444        
    targetPort: 5000  
    nodePort: 30001   